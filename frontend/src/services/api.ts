export const api = {
  // Simulasi fetch data
  fetchUserProfile: async () => {
    return new Promise((resolve) => setTimeout(resolve, 1000));
  },
  submitTugas: async (id: number) => {
    return new Promise((resolve) => setTimeout(resolve, 1000));
  }
};
